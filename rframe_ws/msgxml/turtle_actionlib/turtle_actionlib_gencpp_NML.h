// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         alex on alex-laptop, 2016-11-23 12:44:59 -0500
//  From Template/Script: data_templates/NMLHeaderTemplate.h
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#ifndef _CODEGEN_TURTLE_ACTIONLIB_GENCPPNML_H_
#define _CODEGEN_TURTLE_ACTIONLIB_GENCPPNML_H_

#include <messaging/nml/NMLMessageFactory.h>
//#include <DataTypeInfo.h>
#include <messaging/Message.h>

#include "turtle_actionlib_gencpp_NMLSupport.h"

namespace rframe { class DataTypeInfo; };

#include "ShapeAction.h"
#include "ShapeActionFeedback.h"
#include "ShapeActionGoal.h"
#include "ShapeActionResult.h"
#include "ShapeFeedback.h"
#include "ShapeGoal.h"
#include "ShapeResult.h"
#include "Velocity.h"

namespace turtle_actionlib {

    /** definition of message factory for turtle_actionlib_gencpp */
    class turtle_actionlib_gencppNMLMessageFactory : public NMLMessageFactory
    {
    public:
        /** constructor */
        turtle_actionlib_gencppNMLMessageFactory();
        /** destructor */
        virtual ~turtle_actionlib_gencppNMLMessageFactory();

        /** see  rframe::MessageFactoryInterface */
        virtual void dataTypes(std::vector<DataTypeInfo> & ids);
        /** see  rframe::MessageFactoryInterface */
        virtual int dataType(NMLmsg & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<NMLmsg> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateTransport(NMLmsg * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<MessageBase> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateMessage(MessageBase * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int toTransport( MessageBase & src, NMLmsg & dst);
        /** see  rframe::MessageFactoryInterface */
        virtual int fromTransport( NMLmsg & src, MessageBase & dst);

        /** \return poitner to nml format function */
        virtual NML_FORMAT_PTR FormatPointer();
        /** run constructor for class of type id in the provided buffer */
        virtual int constructBuffer(rframe::ID_TYPE id, void * buffer);
        /** run destructor for class of type id in the buffer */
        virtual int destructBuffer(rframe::ID_TYPE id, void * buffer);
    };

    /** factory allocation function for use when not dynamically loading the factory library  */
    NMLMessageFactory * allocate_turtle_actionlib_gencpp();

    /** NML interface for ShapeAction 

    */
    class ShapeActionNML : public NMLmsg
    {
    public:

        /** constructor */
        ShapeActionNML();
        /** destructor */
        ~ShapeActionNML();

        /** assignment operator */
        ShapeActionNML & operator=(const ShapeActionNML &inobj);
        /** assignment operator for base data class */
        ShapeActionNML & operator=(const ShapeAction &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        ShapeAction nmlPayload;

        /** set payload */
        void payload(const ShapeAction &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<ShapeAction> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        ShapeAction * payload() { return & nmlPayload; };

    //     std::shared_ptr<ShapeAction> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for ShapeActionFeedback 

    */
    class ShapeActionFeedbackNML : public NMLmsg
    {
    public:

        /** constructor */
        ShapeActionFeedbackNML();
        /** destructor */
        ~ShapeActionFeedbackNML();

        /** assignment operator */
        ShapeActionFeedbackNML & operator=(const ShapeActionFeedbackNML &inobj);
        /** assignment operator for base data class */
        ShapeActionFeedbackNML & operator=(const ShapeActionFeedback &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        ShapeActionFeedback nmlPayload;

        /** set payload */
        void payload(const ShapeActionFeedback &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<ShapeActionFeedback> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        ShapeActionFeedback * payload() { return & nmlPayload; };

    //     std::shared_ptr<ShapeActionFeedback> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for ShapeActionGoal 

    */
    class ShapeActionGoalNML : public NMLmsg
    {
    public:

        /** constructor */
        ShapeActionGoalNML();
        /** destructor */
        ~ShapeActionGoalNML();

        /** assignment operator */
        ShapeActionGoalNML & operator=(const ShapeActionGoalNML &inobj);
        /** assignment operator for base data class */
        ShapeActionGoalNML & operator=(const ShapeActionGoal &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        ShapeActionGoal nmlPayload;

        /** set payload */
        void payload(const ShapeActionGoal &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<ShapeActionGoal> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        ShapeActionGoal * payload() { return & nmlPayload; };

    //     std::shared_ptr<ShapeActionGoal> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for ShapeActionResult 

    */
    class ShapeActionResultNML : public NMLmsg
    {
    public:

        /** constructor */
        ShapeActionResultNML();
        /** destructor */
        ~ShapeActionResultNML();

        /** assignment operator */
        ShapeActionResultNML & operator=(const ShapeActionResultNML &inobj);
        /** assignment operator for base data class */
        ShapeActionResultNML & operator=(const ShapeActionResult &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        ShapeActionResult nmlPayload;

        /** set payload */
        void payload(const ShapeActionResult &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<ShapeActionResult> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        ShapeActionResult * payload() { return & nmlPayload; };

    //     std::shared_ptr<ShapeActionResult> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for ShapeFeedback 

    */
    class ShapeFeedbackNML : public NMLmsg
    {
    public:

        /** constructor */
        ShapeFeedbackNML();
        /** destructor */
        ~ShapeFeedbackNML();

        /** assignment operator */
        ShapeFeedbackNML & operator=(const ShapeFeedbackNML &inobj);
        /** assignment operator for base data class */
        ShapeFeedbackNML & operator=(const ShapeFeedback &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        ShapeFeedback nmlPayload;

        /** set payload */
        void payload(const ShapeFeedback &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<ShapeFeedback> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        ShapeFeedback * payload() { return & nmlPayload; };

    //     std::shared_ptr<ShapeFeedback> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for ShapeGoal 

    */
    class ShapeGoalNML : public NMLmsg
    {
    public:

        /** constructor */
        ShapeGoalNML();
        /** destructor */
        ~ShapeGoalNML();

        /** assignment operator */
        ShapeGoalNML & operator=(const ShapeGoalNML &inobj);
        /** assignment operator for base data class */
        ShapeGoalNML & operator=(const ShapeGoal &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        ShapeGoal nmlPayload;

        /** set payload */
        void payload(const ShapeGoal &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<ShapeGoal> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        ShapeGoal * payload() { return & nmlPayload; };

    //     std::shared_ptr<ShapeGoal> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for ShapeResult 

    */
    class ShapeResultNML : public NMLmsg
    {
    public:

        /** constructor */
        ShapeResultNML();
        /** destructor */
        ~ShapeResultNML();

        /** assignment operator */
        ShapeResultNML & operator=(const ShapeResultNML &inobj);
        /** assignment operator for base data class */
        ShapeResultNML & operator=(const ShapeResult &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        ShapeResult nmlPayload;

        /** set payload */
        void payload(const ShapeResult &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<ShapeResult> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        ShapeResult * payload() { return & nmlPayload; };

    //     std::shared_ptr<ShapeResult> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for Velocity 

    */
    class VelocityNML : public NMLmsg
    {
    public:

        /** constructor */
        VelocityNML();
        /** destructor */
        ~VelocityNML();

        /** assignment operator */
        VelocityNML & operator=(const VelocityNML &inobj);
        /** assignment operator for base data class */
        VelocityNML & operator=(const Velocity &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        Velocity nmlPayload;

        /** set payload */
        void payload(const Velocity &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<Velocity> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        Velocity * payload() { return & nmlPayload; };

    //     std::shared_ptr<Velocity> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    // predeclaration of nmlupdate functions (necessary due to namespaces )

    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, turtle_actionlib::ShapeAction & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, turtle_actionlib::ShapeActionFeedback & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, turtle_actionlib::ShapeActionGoal & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, turtle_actionlib::ShapeActionResult & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, turtle_actionlib::ShapeFeedback & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, turtle_actionlib::ShapeGoal & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, turtle_actionlib::ShapeResult & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, turtle_actionlib::Velocity & inobj);

    /** nml format function */
    int turtle_actionlib_gencpp_format(NMLTYPE type, void * buffer, CMS* cms);

}; // end namespace turtle_actionlib

#endif // _TURTLE_ACTIONLIB_GENCPPNML_H_
