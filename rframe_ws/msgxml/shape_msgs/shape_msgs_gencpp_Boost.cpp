// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         yongbo on yongbo-XPS-15-9550, 2017-01-26 15:57:07 -0500
//  From Template/Script: data_templates/DataClassSerializationTemplate.cpp
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#include <stdio.h>

// override the itemName template function used within serialization

#include <common/Log.h>
#include <common/BsonIo.h>
#include <common/SerializationCppHelpers.h>
#include <common/SerializationHelpers.h>

#include <shape_msgs/Mesh.h>
#include <shape_msgs/MeshTriangle.h>
#include <shape_msgs/Plane.h>
#include <shape_msgs/SolidPrimitive.h>

using namespace std;

template<class Archive> void shape_msgs::Mesh::serialize(Archive &ar, const unsigned int fileVersion)
{   

    ar & boost::serialization::make_nvp("triangles",triangles);

    ar & boost::serialization::make_nvp("vertices",vertices);
}

string shape_msgs::Mesh::toXML() const
{
    stringstream str;
    std::cout << "toXML commented out" << std::endl;
    /*
    const shape_msgs::Mesh & robj = (*this);

    boost::archive::xml_oarchive oa(str,boost::archive::no_header);
    oa & boost::serialization::make_nvp("Mesh",robj);

    */
    string s = str.str();

    return s;
};

bool shape_msgs::Mesh::fromXML(const string &str)
{
    bool retval = true;
        std::cout << "fromXML commented out" << std::endl;
    /*
    stringstream sstr(str);
    shape_msgs::Mesh & robj = (*this);

    try
    {
        boost::archive::xml_iarchive oa(sstr,boost::archive::no_header);
        oa & boost::serialization::make_nvp("Mesh",robj);
    }
    catch (exception & e)
    {
        retval = false;
    }

    */
    return retval;
};

GENERIC_SERIALIZATION_ENTRYPOINTS(shape_msgs::Mesh); // defined in common/Common.h

INSTANTIATE_BOOST_SERIALIZATION(shape_msgs::Mesh); // defined in common/SerialzationHelpers.h

template<class Archive> void shape_msgs::MeshTriangle::serialize(Archive &ar, const unsigned int fileVersion)
{   
    // manually serialize arrays as the boost serialization code doesnt compile againt the type registry due to namespace issue
    {                                                                                            
        int count = 3;
        unsigned int * t = vertex_indices.data();
        /*        string tempName;
        rframe::TypeRegistry &reg = rframe::TypeRegistry::instance();

        reg.lookupXMLName(tempName,typeid(unsigned int).name());*/

        while (0 < count--)
            ar & boost::serialization::make_nvp("item",*t++);
    }

}

string shape_msgs::MeshTriangle::toXML() const
{
    stringstream str;
    std::cout << "toXML commented out" << std::endl;
    /*
    const shape_msgs::MeshTriangle & robj = (*this);

    boost::archive::xml_oarchive oa(str,boost::archive::no_header);
    oa & boost::serialization::make_nvp("MeshTriangle",robj);

    */
    string s = str.str();

    return s;
};

bool shape_msgs::MeshTriangle::fromXML(const string &str)
{
    bool retval = true;
        std::cout << "fromXML commented out" << std::endl;
    /*
    stringstream sstr(str);
    shape_msgs::MeshTriangle & robj = (*this);

    try
    {
        boost::archive::xml_iarchive oa(sstr,boost::archive::no_header);
        oa & boost::serialization::make_nvp("MeshTriangle",robj);
    }
    catch (exception & e)
    {
        retval = false;
    }

    */
    return retval;
};

GENERIC_SERIALIZATION_ENTRYPOINTS(shape_msgs::MeshTriangle); // defined in common/Common.h

INSTANTIATE_BOOST_SERIALIZATION(shape_msgs::MeshTriangle); // defined in common/SerialzationHelpers.h

template<class Archive> void shape_msgs::Plane::serialize(Archive &ar, const unsigned int fileVersion)
{   
    // manually serialize arrays as the boost serialization code doesnt compile againt the type registry due to namespace issue
    {                                                                                            
        int count = 4;
        double * t = coef.data();
        /*        string tempName;
        rframe::TypeRegistry &reg = rframe::TypeRegistry::instance();

        reg.lookupXMLName(tempName,typeid(double).name());*/

        while (0 < count--)
            ar & boost::serialization::make_nvp("item",*t++);
    }

}

string shape_msgs::Plane::toXML() const
{
    stringstream str;
    std::cout << "toXML commented out" << std::endl;
    /*
    const shape_msgs::Plane & robj = (*this);

    boost::archive::xml_oarchive oa(str,boost::archive::no_header);
    oa & boost::serialization::make_nvp("Plane",robj);

    */
    string s = str.str();

    return s;
};

bool shape_msgs::Plane::fromXML(const string &str)
{
    bool retval = true;
        std::cout << "fromXML commented out" << std::endl;
    /*
    stringstream sstr(str);
    shape_msgs::Plane & robj = (*this);

    try
    {
        boost::archive::xml_iarchive oa(sstr,boost::archive::no_header);
        oa & boost::serialization::make_nvp("Plane",robj);
    }
    catch (exception & e)
    {
        retval = false;
    }

    */
    return retval;
};

GENERIC_SERIALIZATION_ENTRYPOINTS(shape_msgs::Plane); // defined in common/Common.h

INSTANTIATE_BOOST_SERIALIZATION(shape_msgs::Plane); // defined in common/SerialzationHelpers.h

template<class Archive> void shape_msgs::SolidPrimitive::serialize(Archive &ar, const unsigned int fileVersion)
{   

    ar & boost::serialization::make_nvp("type",type);

    ar & boost::serialization::make_nvp("dimensions",dimensions);
}

string shape_msgs::SolidPrimitive::toXML() const
{
    stringstream str;
    std::cout << "toXML commented out" << std::endl;
    /*
    const shape_msgs::SolidPrimitive & robj = (*this);

    boost::archive::xml_oarchive oa(str,boost::archive::no_header);
    oa & boost::serialization::make_nvp("SolidPrimitive",robj);

    */
    string s = str.str();

    return s;
};

bool shape_msgs::SolidPrimitive::fromXML(const string &str)
{
    bool retval = true;
        std::cout << "fromXML commented out" << std::endl;
    /*
    stringstream sstr(str);
    shape_msgs::SolidPrimitive & robj = (*this);

    try
    {
        boost::archive::xml_iarchive oa(sstr,boost::archive::no_header);
        oa & boost::serialization::make_nvp("SolidPrimitive",robj);
    }
    catch (exception & e)
    {
        retval = false;
    }

    */
    return retval;
};

GENERIC_SERIALIZATION_ENTRYPOINTS(shape_msgs::SolidPrimitive); // defined in common/Common.h

INSTANTIATE_BOOST_SERIALIZATION(shape_msgs::SolidPrimitive); // defined in common/SerialzationHelpers.h

