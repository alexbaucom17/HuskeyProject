// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         alex on alex-laptop, 2016-11-23 12:44:55 -0500
//  From Template/Script: data_templates/ROSTemplate.h.rb
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#ifndef _CODEGEN_SMACH_MSGS_GENCPP_ROS_H_
#define _CODEGEN_SMACH_MSGS_GENCPP_ROS_H_

#include <opensource/ros/serialization.h>
#include <opensource/ros/message_traits.h>
#include <opensource/ros/message_operations.h>

#include <messaging/ros/ROSMessageFactory.h>
#include <messaging/Message.h>

namespace rframe { class DataTypeInfo; };

#include "smach_msgs_gencpp_Library.h"

#include "std_msgs/std_msgs_gencpp_ROS.h"
#include "std_srvs/std_srvs_gencpp_ROS.h"

namespace smach_msgs { 

    /** definition of message factory for smach_msgs_gencpp */
    class smach_msgs_gencppROSMessageFactory : public ROSMessageFactory
    {
    public:
        /** constructor */
        smach_msgs_gencppROSMessageFactory();
        /** destructor */
        virtual ~smach_msgs_gencppROSMessageFactory();

        /** see  rframe::MessageFactoryInterface */
        virtual void dataTypes(std::vector<DataTypeInfo> & ids);
        /** see  rframe::MessageFactoryInterface */
        virtual int dataType(ros::SerializedMessage & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<ros::SerializedMessage> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateTransport(ros::SerializedMessage * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<MessageBase> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateMessage(MessageBase * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int toTransport( MessageBase & src, ros::SerializedMessage & dst);
        /** see  rframe::MessageFactoryInterface */
        virtual int fromTransport( ros::SerializedMessage & src, MessageBase & dst);

		/** see rframe::ROSMessageFactory */
        virtual int typeName(std::string & str, rframe::ID_TYPE id);
		/** see rframe::ROSMessageFactory */
        virtual int desc(std::string & str, rframe::ID_TYPE id);
		/** see rframe::ROSMessageFactory */
        virtual int md5String(std::string & str, rframe::ID_TYPE id);
        /** see rframe::ROSMessageFactory */
        virtual int md5(unsigned long long & highOrder, unsigned long long & lowOrder, rframe::ID_TYPE id);
        /** see rframe::ROSMessageFactory */
		int updateHeader(rframe::MessageBase &msg, unsigned int seqNo);
    };

    /** factory allocation function for use when not dynamically loading the factory library  */
    ROSMessageFactory * allocate_smach_msgs_gencpp();

}; // end namespace smach_msgs

// declare ros serialization templates
namespace ros
{
  namespace serialization
  {  

      template <> struct Serializer<smach_msgs::SmachContainerInitialStatusCmd>
      {
          template <typename Stream> inline static void write(Stream & stream, const smach_msgs::SmachContainerInitialStatusCmd & t)
          { 
              stream.next(t.path); 
              stream.next(t.initial_states); 
              stream.next(t.local_data); 
          };

          template <typename Stream> inline static void read(Stream & stream, smach_msgs::SmachContainerInitialStatusCmd & t)
          { 
              stream.next(t.path); 
              stream.next(t.initial_states); 
              stream.next(t.local_data); 
          };

          inline static uint32_t serializedLength(const smach_msgs::SmachContainerInitialStatusCmd & t)
          { 
              uint32_t size = 0;

              size += serializationLength(t.path); 
              size += serializationLength(t.initial_states); 
              size += serializationLength(t.local_data); 
              return size;
          };
      }; // end struct Serializer<smach_msgs::SmachContainerInitialStatusCmd>

      template <> struct Serializer<smach_msgs::SmachContainerStatus>
      {
          template <typename Stream> inline static void write(Stream & stream, const smach_msgs::SmachContainerStatus & t)
          { 
              stream.next(t.header); 
              stream.next(t.path); 
              stream.next(t.initial_states); 
              stream.next(t.active_states); 
              stream.next(t.local_data); 
              stream.next(t.info); 
          };

          template <typename Stream> inline static void read(Stream & stream, smach_msgs::SmachContainerStatus & t)
          { 
              stream.next(t.header); 
              stream.next(t.path); 
              stream.next(t.initial_states); 
              stream.next(t.active_states); 
              stream.next(t.local_data); 
              stream.next(t.info); 
          };

          inline static uint32_t serializedLength(const smach_msgs::SmachContainerStatus & t)
          { 
              uint32_t size = 0;

              size += serializationLength(t.header); 
              size += serializationLength(t.path); 
              size += serializationLength(t.initial_states); 
              size += serializationLength(t.active_states); 
              size += serializationLength(t.local_data); 
              size += serializationLength(t.info); 
              return size;
          };
      }; // end struct Serializer<smach_msgs::SmachContainerStatus>

      template <> struct Serializer<smach_msgs::SmachContainerStructure>
      {
          template <typename Stream> inline static void write(Stream & stream, const smach_msgs::SmachContainerStructure & t)
          { 
              stream.next(t.header); 
              stream.next(t.path); 
              stream.next(t.children); 
              stream.next(t.internal_outcomes); 
              stream.next(t.outcomes_from); 
              stream.next(t.outcomes_to); 
              stream.next(t.container_outcomes); 
          };

          template <typename Stream> inline static void read(Stream & stream, smach_msgs::SmachContainerStructure & t)
          { 
              stream.next(t.header); 
              stream.next(t.path); 
              stream.next(t.children); 
              stream.next(t.internal_outcomes); 
              stream.next(t.outcomes_from); 
              stream.next(t.outcomes_to); 
              stream.next(t.container_outcomes); 
          };

          inline static uint32_t serializedLength(const smach_msgs::SmachContainerStructure & t)
          { 
              uint32_t size = 0;

              size += serializationLength(t.header); 
              size += serializationLength(t.path); 
              size += serializationLength(t.children); 
              size += serializationLength(t.internal_outcomes); 
              size += serializationLength(t.outcomes_from); 
              size += serializationLength(t.outcomes_to); 
              size += serializationLength(t.container_outcomes); 
              return size;
          };
      }; // end struct Serializer<smach_msgs::SmachContainerStructure>

  }; // namespace serialization

  namespace message_traits
  {

      template <> struct MD5Sum<smach_msgs::SmachContainerInitialStatusCmd>
      {

          static const char * value() 
          { 
              return "45f8cf31fc29b829db77f23001f788d6"; 
          };

          static const uint64_t static_value1 = 0x45f8cf31fc29b829ULL;
          static const uint64_t static_value2 = 0xdb77f23001f788d6ULL;
      }; // end struct MD5Sum<smach_msgs::SmachContainerInitialStatusCmd>

      template <> struct DataType<smach_msgs::SmachContainerInitialStatusCmd>
      {
          static const char * value() { return "smach_msgs/SmachContainerInitialStatusCmd";};
      }; // end DataType

      template <> struct Definition<smach_msgs::SmachContainerInitialStatusCmd>
      {
          static const char * value()
          { 
              return "# The path to the node in the server\nstring path\n\n# The desired initial state(s)\nstring[] initial_states\n\n# Initial values for the local user data of the state machine\n# A pickled user data structure\n# i.e. the UserData's internal dictionary\nstring local_data\n";
          };
      };

      template <> struct MD5Sum<smach_msgs::SmachContainerStatus>
      {

          static const char * value() 
          { 
              return "5ba2bb79ac19e3842d562a191f2a675b"; 
          };

          static const uint64_t static_value1 = 0x5ba2bb79ac19e384ULL;
          static const uint64_t static_value2 = 0x2d562a191f2a675bULL;
      }; // end struct MD5Sum<smach_msgs::SmachContainerStatus>

      template <> struct DataType<smach_msgs::SmachContainerStatus>
      {
          static const char * value() { return "smach_msgs/SmachContainerStatus";};
      }; // end DataType

      template <> struct Definition<smach_msgs::SmachContainerStatus>
      {
          static const char * value()
          { 
              return "Header header\n\n# The path to this node in the server\nstring path\n\n# The initial state description\n# Effects an arc from the top state to each one\nstring[] initial_states\n\n# The current state description\nstring[] active_states\n\n# A pickled user data structure\n# i.e. the UserData's internal dictionary\nstring local_data\n\n# Debugging info string\nstring info\n\n================================================================================\nMSG: std_msgs/Header\n# Standard metadata for higher-level stamped data types.\n# This is generally used to communicate timestamped data \n# in a particular coordinate frame.\n# \n# sequence ID: consecutively increasing ID \nuint32 seq\n#Two-integer timestamp that is expressed as:\n# * stamp.sec: seconds (stamp_secs) since epoch (in Python the variable is called 'secs')\n# * stamp.nsec: nanoseconds since stamp_secs (in Python the variable is called 'nsecs')\n# time-handling sugar is provided by the client library\ntime stamp\n#Frame this data is associated with\n# 0: no frame\n# 1: global frame\nstring frame_id\n";
          };
      };

      template <> struct MD5Sum<smach_msgs::SmachContainerStructure>
      {

          static const char * value() 
          { 
              return "3d3d1e0d0f99779ee9e58101a5dcf7ea"; 
          };

          static const uint64_t static_value1 = 0x3d3d1e0d0f99779eULL;
          static const uint64_t static_value2 = 0xe9e58101a5dcf7eaULL;
      }; // end struct MD5Sum<smach_msgs::SmachContainerStructure>

      template <> struct DataType<smach_msgs::SmachContainerStructure>
      {
          static const char * value() { return "smach_msgs/SmachContainerStructure";};
      }; // end DataType

      template <> struct Definition<smach_msgs::SmachContainerStructure>
      {
          static const char * value()
          { 
              return "Header header\n\n# The path to this node in the server\nstring path\n\n# The children of this node\nstring[] children\n\n# The outcome edges\n# Each index across these arrays denote one edge\nstring[] internal_outcomes\nstring[] outcomes_from\nstring[] outcomes_to\n\n# The potential outcomes from this container\nstring[] container_outcomes\n\n================================================================================\nMSG: std_msgs/Header\n# Standard metadata for higher-level stamped data types.\n# This is generally used to communicate timestamped data \n# in a particular coordinate frame.\n# \n# sequence ID: consecutively increasing ID \nuint32 seq\n#Two-integer timestamp that is expressed as:\n# * stamp.sec: seconds (stamp_secs) since epoch (in Python the variable is called 'secs')\n# * stamp.nsec: nanoseconds since stamp_secs (in Python the variable is called 'nsecs')\n# time-handling sugar is provided by the client library\ntime stamp\n#Frame this data is associated with\n# 0: no frame\n# 1: global frame\nstring frame_id\n";
          };
      };

  }; // end namespace message_traits

}; // namespace ros

#endif // _CODEGEN_SMACH_MSGS_GENCPP_ROS_H_

