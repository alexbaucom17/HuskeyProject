// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         yongbo on yongbo-XPS-15-9550, 2017-01-26 15:30:54 -0500
//  From Template/Script: data_templates/NMLHeaderTemplate.h
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#ifndef _CODEGEN_NAVFN_GENCPPNML_H_
#define _CODEGEN_NAVFN_GENCPPNML_H_

#include <messaging/nml/NMLMessageFactory.h>
//#include <DataTypeInfo.h>
#include <messaging/Message.h>

#include "navfn_gencpp_NMLSupport.h"

namespace rframe { class DataTypeInfo; };

#include "MakeNavPlanRequest.h"
#include "MakeNavPlanResponse.h"
#include "MakeNavPlan.h"
#include "SetCostmapRequest.h"
#include "SetCostmapResponse.h"
#include "SetCostmap.h"

namespace navfn {

    /** definition of message factory for navfn_gencpp */
    class navfn_gencppNMLMessageFactory : public NMLMessageFactory
    {
    public:
        /** constructor */
        navfn_gencppNMLMessageFactory();
        /** destructor */
        virtual ~navfn_gencppNMLMessageFactory();

        /** see  rframe::MessageFactoryInterface */
        virtual void dataTypes(std::vector<DataTypeInfo> & ids);
        /** see  rframe::MessageFactoryInterface */
        virtual int dataType(NMLmsg & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<NMLmsg> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateTransport(NMLmsg * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<MessageBase> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateMessage(MessageBase * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int toTransport( MessageBase & src, NMLmsg & dst);
        /** see  rframe::MessageFactoryInterface */
        virtual int fromTransport( NMLmsg & src, MessageBase & dst);

        /** \return poitner to nml format function */
        virtual NML_FORMAT_PTR FormatPointer();
        /** run constructor for class of type id in the provided buffer */
        virtual int constructBuffer(rframe::ID_TYPE id, void * buffer);
        /** run destructor for class of type id in the buffer */
        virtual int destructBuffer(rframe::ID_TYPE id, void * buffer);
    };

    /** factory allocation function for use when not dynamically loading the factory library  */
    NMLMessageFactory * allocate_navfn_gencpp();

    /** NML interface for MakeNavPlanRequest 

    */
    class MakeNavPlanRequestNML : public NMLmsg
    {
    public:

        /** constructor */
        MakeNavPlanRequestNML();
        /** destructor */
        ~MakeNavPlanRequestNML();

        /** assignment operator */
        MakeNavPlanRequestNML & operator=(const MakeNavPlanRequestNML &inobj);
        /** assignment operator for base data class */
        MakeNavPlanRequestNML & operator=(const MakeNavPlanRequest &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        MakeNavPlanRequest nmlPayload;

        /** set payload */
        void payload(const MakeNavPlanRequest &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<MakeNavPlanRequest> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        MakeNavPlanRequest * payload() { return & nmlPayload; };

    //     std::shared_ptr<MakeNavPlanRequest> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for MakeNavPlanResponse 

    */
    class MakeNavPlanResponseNML : public NMLmsg
    {
    public:

        /** constructor */
        MakeNavPlanResponseNML();
        /** destructor */
        ~MakeNavPlanResponseNML();

        /** assignment operator */
        MakeNavPlanResponseNML & operator=(const MakeNavPlanResponseNML &inobj);
        /** assignment operator for base data class */
        MakeNavPlanResponseNML & operator=(const MakeNavPlanResponse &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        MakeNavPlanResponse nmlPayload;

        /** set payload */
        void payload(const MakeNavPlanResponse &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<MakeNavPlanResponse> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        MakeNavPlanResponse * payload() { return & nmlPayload; };

    //     std::shared_ptr<MakeNavPlanResponse> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for MakeNavPlan 

    */
    class MakeNavPlanNML : public NMLmsg
    {
    public:

        /** constructor */
        MakeNavPlanNML();
        /** destructor */
        ~MakeNavPlanNML();

        /** assignment operator */
        MakeNavPlanNML & operator=(const MakeNavPlanNML &inobj);
        /** assignment operator for base data class */
        MakeNavPlanNML & operator=(const MakeNavPlan &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        MakeNavPlan nmlPayload;

        /** set payload */
        void payload(const MakeNavPlan &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<MakeNavPlan> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        MakeNavPlan * payload() { return & nmlPayload; };

    //     std::shared_ptr<MakeNavPlan> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for SetCostmapRequest 

    */
    class SetCostmapRequestNML : public NMLmsg
    {
    public:

        /** constructor */
        SetCostmapRequestNML();
        /** destructor */
        ~SetCostmapRequestNML();

        /** assignment operator */
        SetCostmapRequestNML & operator=(const SetCostmapRequestNML &inobj);
        /** assignment operator for base data class */
        SetCostmapRequestNML & operator=(const SetCostmapRequest &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        SetCostmapRequest nmlPayload;

        /** set payload */
        void payload(const SetCostmapRequest &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<SetCostmapRequest> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        SetCostmapRequest * payload() { return & nmlPayload; };

    //     std::shared_ptr<SetCostmapRequest> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for SetCostmapResponse 

    */
    class SetCostmapResponseNML : public NMLmsg
    {
    public:

        /** constructor */
        SetCostmapResponseNML();
        /** destructor */
        ~SetCostmapResponseNML();

        /** assignment operator */
        SetCostmapResponseNML & operator=(const SetCostmapResponseNML &inobj);
        /** assignment operator for base data class */
        SetCostmapResponseNML & operator=(const SetCostmapResponse &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        SetCostmapResponse nmlPayload;

        /** set payload */
        void payload(const SetCostmapResponse &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<SetCostmapResponse> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        SetCostmapResponse * payload() { return & nmlPayload; };

    //     std::shared_ptr<SetCostmapResponse> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for SetCostmap 

    */
    class SetCostmapNML : public NMLmsg
    {
    public:

        /** constructor */
        SetCostmapNML();
        /** destructor */
        ~SetCostmapNML();

        /** assignment operator */
        SetCostmapNML & operator=(const SetCostmapNML &inobj);
        /** assignment operator for base data class */
        SetCostmapNML & operator=(const SetCostmap &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        SetCostmap nmlPayload;

        /** set payload */
        void payload(const SetCostmap &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<SetCostmap> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        SetCostmap * payload() { return & nmlPayload; };

    //     std::shared_ptr<SetCostmap> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    // predeclaration of nmlupdate functions (necessary due to namespaces )

    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, navfn::MakeNavPlanRequest & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, navfn::MakeNavPlanResponse & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, navfn::MakeNavPlan & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, navfn::SetCostmapRequest & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, navfn::SetCostmapResponse & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, navfn::SetCostmap & inobj);

    /** nml format function */
    int navfn_gencpp_format(NMLTYPE type, void * buffer, CMS* cms);

}; // end namespace navfn

#endif // _NAVFN_GENCPPNML_H_
