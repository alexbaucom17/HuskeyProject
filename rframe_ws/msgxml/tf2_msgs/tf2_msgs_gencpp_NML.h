// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         yongbo on yongbo-XPS-15-9550, 2017-01-26 15:57:08 -0500
//  From Template/Script: data_templates/NMLHeaderTemplate.h
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#ifndef _CODEGEN_TF2_MSGS_GENCPPNML_H_
#define _CODEGEN_TF2_MSGS_GENCPPNML_H_

#include <messaging/nml/NMLMessageFactory.h>
//#include <DataTypeInfo.h>
#include <messaging/Message.h>

#include "tf2_msgs_gencpp_NMLSupport.h"

namespace rframe { class DataTypeInfo; };

#include "LookupTransformAction.h"
#include "LookupTransformActionFeedback.h"
#include "LookupTransformActionGoal.h"
#include "LookupTransformActionResult.h"
#include "LookupTransformFeedback.h"
#include "LookupTransformGoal.h"
#include "LookupTransformResult.h"
#include "TF2Error.h"
#include "TFMessage.h"
#include "FrameGraphRequest.h"
#include "FrameGraphResponse.h"
#include "FrameGraph.h"

namespace tf2_msgs {

    /** definition of message factory for tf2_msgs_gencpp */
    class tf2_msgs_gencppNMLMessageFactory : public NMLMessageFactory
    {
    public:
        /** constructor */
        tf2_msgs_gencppNMLMessageFactory();
        /** destructor */
        virtual ~tf2_msgs_gencppNMLMessageFactory();

        /** see  rframe::MessageFactoryInterface */
        virtual void dataTypes(std::vector<DataTypeInfo> & ids);
        /** see  rframe::MessageFactoryInterface */
        virtual int dataType(NMLmsg & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<NMLmsg> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateTransport(NMLmsg * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<MessageBase> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateMessage(MessageBase * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int toTransport( MessageBase & src, NMLmsg & dst);
        /** see  rframe::MessageFactoryInterface */
        virtual int fromTransport( NMLmsg & src, MessageBase & dst);

        /** \return poitner to nml format function */
        virtual NML_FORMAT_PTR FormatPointer();
        /** run constructor for class of type id in the provided buffer */
        virtual int constructBuffer(rframe::ID_TYPE id, void * buffer);
        /** run destructor for class of type id in the buffer */
        virtual int destructBuffer(rframe::ID_TYPE id, void * buffer);
    };

    /** factory allocation function for use when not dynamically loading the factory library  */
    NMLMessageFactory * allocate_tf2_msgs_gencpp();

    /** NML interface for LookupTransformAction 

    */
    class LookupTransformActionNML : public NMLmsg
    {
    public:

        /** constructor */
        LookupTransformActionNML();
        /** destructor */
        ~LookupTransformActionNML();

        /** assignment operator */
        LookupTransformActionNML & operator=(const LookupTransformActionNML &inobj);
        /** assignment operator for base data class */
        LookupTransformActionNML & operator=(const LookupTransformAction &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        LookupTransformAction nmlPayload;

        /** set payload */
        void payload(const LookupTransformAction &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<LookupTransformAction> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        LookupTransformAction * payload() { return & nmlPayload; };

    //     std::shared_ptr<LookupTransformAction> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for LookupTransformActionFeedback 

    */
    class LookupTransformActionFeedbackNML : public NMLmsg
    {
    public:

        /** constructor */
        LookupTransformActionFeedbackNML();
        /** destructor */
        ~LookupTransformActionFeedbackNML();

        /** assignment operator */
        LookupTransformActionFeedbackNML & operator=(const LookupTransformActionFeedbackNML &inobj);
        /** assignment operator for base data class */
        LookupTransformActionFeedbackNML & operator=(const LookupTransformActionFeedback &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        LookupTransformActionFeedback nmlPayload;

        /** set payload */
        void payload(const LookupTransformActionFeedback &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<LookupTransformActionFeedback> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        LookupTransformActionFeedback * payload() { return & nmlPayload; };

    //     std::shared_ptr<LookupTransformActionFeedback> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for LookupTransformActionGoal 

    */
    class LookupTransformActionGoalNML : public NMLmsg
    {
    public:

        /** constructor */
        LookupTransformActionGoalNML();
        /** destructor */
        ~LookupTransformActionGoalNML();

        /** assignment operator */
        LookupTransformActionGoalNML & operator=(const LookupTransformActionGoalNML &inobj);
        /** assignment operator for base data class */
        LookupTransformActionGoalNML & operator=(const LookupTransformActionGoal &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        LookupTransformActionGoal nmlPayload;

        /** set payload */
        void payload(const LookupTransformActionGoal &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<LookupTransformActionGoal> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        LookupTransformActionGoal * payload() { return & nmlPayload; };

    //     std::shared_ptr<LookupTransformActionGoal> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for LookupTransformActionResult 

    */
    class LookupTransformActionResultNML : public NMLmsg
    {
    public:

        /** constructor */
        LookupTransformActionResultNML();
        /** destructor */
        ~LookupTransformActionResultNML();

        /** assignment operator */
        LookupTransformActionResultNML & operator=(const LookupTransformActionResultNML &inobj);
        /** assignment operator for base data class */
        LookupTransformActionResultNML & operator=(const LookupTransformActionResult &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        LookupTransformActionResult nmlPayload;

        /** set payload */
        void payload(const LookupTransformActionResult &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<LookupTransformActionResult> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        LookupTransformActionResult * payload() { return & nmlPayload; };

    //     std::shared_ptr<LookupTransformActionResult> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for LookupTransformFeedback 

    */
    class LookupTransformFeedbackNML : public NMLmsg
    {
    public:

        /** constructor */
        LookupTransformFeedbackNML();
        /** destructor */
        ~LookupTransformFeedbackNML();

        /** assignment operator */
        LookupTransformFeedbackNML & operator=(const LookupTransformFeedbackNML &inobj);
        /** assignment operator for base data class */
        LookupTransformFeedbackNML & operator=(const LookupTransformFeedback &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        LookupTransformFeedback nmlPayload;

        /** set payload */
        void payload(const LookupTransformFeedback &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<LookupTransformFeedback> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        LookupTransformFeedback * payload() { return & nmlPayload; };

    //     std::shared_ptr<LookupTransformFeedback> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for LookupTransformGoal 

    */
    class LookupTransformGoalNML : public NMLmsg
    {
    public:

        /** constructor */
        LookupTransformGoalNML();
        /** destructor */
        ~LookupTransformGoalNML();

        /** assignment operator */
        LookupTransformGoalNML & operator=(const LookupTransformGoalNML &inobj);
        /** assignment operator for base data class */
        LookupTransformGoalNML & operator=(const LookupTransformGoal &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        LookupTransformGoal nmlPayload;

        /** set payload */
        void payload(const LookupTransformGoal &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<LookupTransformGoal> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        LookupTransformGoal * payload() { return & nmlPayload; };

    //     std::shared_ptr<LookupTransformGoal> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for LookupTransformResult 

    */
    class LookupTransformResultNML : public NMLmsg
    {
    public:

        /** constructor */
        LookupTransformResultNML();
        /** destructor */
        ~LookupTransformResultNML();

        /** assignment operator */
        LookupTransformResultNML & operator=(const LookupTransformResultNML &inobj);
        /** assignment operator for base data class */
        LookupTransformResultNML & operator=(const LookupTransformResult &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        LookupTransformResult nmlPayload;

        /** set payload */
        void payload(const LookupTransformResult &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<LookupTransformResult> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        LookupTransformResult * payload() { return & nmlPayload; };

    //     std::shared_ptr<LookupTransformResult> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for TF2Error 

    */
    class TF2ErrorNML : public NMLmsg
    {
    public:

        /** constructor */
        TF2ErrorNML();
        /** destructor */
        ~TF2ErrorNML();

        /** assignment operator */
        TF2ErrorNML & operator=(const TF2ErrorNML &inobj);
        /** assignment operator for base data class */
        TF2ErrorNML & operator=(const TF2Error &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        TF2Error nmlPayload;

        /** set payload */
        void payload(const TF2Error &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<TF2Error> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        TF2Error * payload() { return & nmlPayload; };

    //     std::shared_ptr<TF2Error> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for TFMessage 

    */
    class TFMessageNML : public NMLmsg
    {
    public:

        /** constructor */
        TFMessageNML();
        /** destructor */
        ~TFMessageNML();

        /** assignment operator */
        TFMessageNML & operator=(const TFMessageNML &inobj);
        /** assignment operator for base data class */
        TFMessageNML & operator=(const TFMessage &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        TFMessage nmlPayload;

        /** set payload */
        void payload(const TFMessage &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<TFMessage> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        TFMessage * payload() { return & nmlPayload; };

    //     std::shared_ptr<TFMessage> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for FrameGraphRequest 

    */
    class FrameGraphRequestNML : public NMLmsg
    {
    public:

        /** constructor */
        FrameGraphRequestNML();
        /** destructor */
        ~FrameGraphRequestNML();

        /** assignment operator */
        FrameGraphRequestNML & operator=(const FrameGraphRequestNML &inobj);
        /** assignment operator for base data class */
        FrameGraphRequestNML & operator=(const FrameGraphRequest &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        FrameGraphRequest nmlPayload;

        /** set payload */
        void payload(const FrameGraphRequest &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<FrameGraphRequest> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        FrameGraphRequest * payload() { return & nmlPayload; };

    //     std::shared_ptr<FrameGraphRequest> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for FrameGraphResponse 

    */
    class FrameGraphResponseNML : public NMLmsg
    {
    public:

        /** constructor */
        FrameGraphResponseNML();
        /** destructor */
        ~FrameGraphResponseNML();

        /** assignment operator */
        FrameGraphResponseNML & operator=(const FrameGraphResponseNML &inobj);
        /** assignment operator for base data class */
        FrameGraphResponseNML & operator=(const FrameGraphResponse &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        FrameGraphResponse nmlPayload;

        /** set payload */
        void payload(const FrameGraphResponse &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<FrameGraphResponse> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        FrameGraphResponse * payload() { return & nmlPayload; };

    //     std::shared_ptr<FrameGraphResponse> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for FrameGraph 

    */
    class FrameGraphNML : public NMLmsg
    {
    public:

        /** constructor */
        FrameGraphNML();
        /** destructor */
        ~FrameGraphNML();

        /** assignment operator */
        FrameGraphNML & operator=(const FrameGraphNML &inobj);
        /** assignment operator for base data class */
        FrameGraphNML & operator=(const FrameGraph &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        FrameGraph nmlPayload;

        /** set payload */
        void payload(const FrameGraph &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<FrameGraph> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        FrameGraph * payload() { return & nmlPayload; };

    //     std::shared_ptr<FrameGraph> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    // predeclaration of nmlupdate functions (necessary due to namespaces )

    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::LookupTransformAction & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::LookupTransformActionFeedback & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::LookupTransformActionGoal & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::LookupTransformActionResult & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::LookupTransformFeedback & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::LookupTransformGoal & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::LookupTransformResult & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::TF2Error & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::TFMessage & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::FrameGraphRequest & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::FrameGraphResponse & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, tf2_msgs::FrameGraph & inobj);

    /** nml format function */
    int tf2_msgs_gencpp_format(NMLTYPE type, void * buffer, CMS* cms);

}; // end namespace tf2_msgs

#endif // _TF2_MSGS_GENCPPNML_H_
