// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         yongbo on yongbo-XPS-15-9550, 2017-01-26 15:57:04 -0500
//  From Template/Script: data_templates/NMLHeaderTemplate.h
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#ifndef _CODEGEN_DIAGNOSTIC_MSGS_GENCPPNML_H_
#define _CODEGEN_DIAGNOSTIC_MSGS_GENCPPNML_H_

#include <messaging/nml/NMLMessageFactory.h>
//#include <DataTypeInfo.h>
#include <messaging/Message.h>

#include "diagnostic_msgs_gencpp_NMLSupport.h"

namespace rframe { class DataTypeInfo; };

#include "DiagnosticArray.h"
#include "DiagnosticStatus.h"
#include "KeyValue.h"
#include "AddDiagnosticsRequest.h"
#include "AddDiagnosticsResponse.h"
#include "AddDiagnostics.h"
#include "SelfTestRequest.h"
#include "SelfTestResponse.h"
#include "SelfTest.h"

namespace diagnostic_msgs {

    /** definition of message factory for diagnostic_msgs_gencpp */
    class diagnostic_msgs_gencppNMLMessageFactory : public NMLMessageFactory
    {
    public:
        /** constructor */
        diagnostic_msgs_gencppNMLMessageFactory();
        /** destructor */
        virtual ~diagnostic_msgs_gencppNMLMessageFactory();

        /** see  rframe::MessageFactoryInterface */
        virtual void dataTypes(std::vector<DataTypeInfo> & ids);
        /** see  rframe::MessageFactoryInterface */
        virtual int dataType(NMLmsg & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<NMLmsg> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateTransport(NMLmsg * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<MessageBase> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateMessage(MessageBase * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int toTransport( MessageBase & src, NMLmsg & dst);
        /** see  rframe::MessageFactoryInterface */
        virtual int fromTransport( NMLmsg & src, MessageBase & dst);

        /** \return poitner to nml format function */
        virtual NML_FORMAT_PTR FormatPointer();
        /** run constructor for class of type id in the provided buffer */
        virtual int constructBuffer(rframe::ID_TYPE id, void * buffer);
        /** run destructor for class of type id in the buffer */
        virtual int destructBuffer(rframe::ID_TYPE id, void * buffer);
    };

    /** factory allocation function for use when not dynamically loading the factory library  */
    NMLMessageFactory * allocate_diagnostic_msgs_gencpp();

    /** NML interface for DiagnosticArray 

    */
    class DiagnosticArrayNML : public NMLmsg
    {
    public:

        /** constructor */
        DiagnosticArrayNML();
        /** destructor */
        ~DiagnosticArrayNML();

        /** assignment operator */
        DiagnosticArrayNML & operator=(const DiagnosticArrayNML &inobj);
        /** assignment operator for base data class */
        DiagnosticArrayNML & operator=(const DiagnosticArray &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        DiagnosticArray nmlPayload;

        /** set payload */
        void payload(const DiagnosticArray &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<DiagnosticArray> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        DiagnosticArray * payload() { return & nmlPayload; };

    //     std::shared_ptr<DiagnosticArray> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for DiagnosticStatus 

    */
    class DiagnosticStatusNML : public NMLmsg
    {
    public:

        /** constructor */
        DiagnosticStatusNML();
        /** destructor */
        ~DiagnosticStatusNML();

        /** assignment operator */
        DiagnosticStatusNML & operator=(const DiagnosticStatusNML &inobj);
        /** assignment operator for base data class */
        DiagnosticStatusNML & operator=(const DiagnosticStatus &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        DiagnosticStatus nmlPayload;

        /** set payload */
        void payload(const DiagnosticStatus &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<DiagnosticStatus> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        DiagnosticStatus * payload() { return & nmlPayload; };

    //     std::shared_ptr<DiagnosticStatus> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for KeyValue 

    */
    class KeyValueNML : public NMLmsg
    {
    public:

        /** constructor */
        KeyValueNML();
        /** destructor */
        ~KeyValueNML();

        /** assignment operator */
        KeyValueNML & operator=(const KeyValueNML &inobj);
        /** assignment operator for base data class */
        KeyValueNML & operator=(const KeyValue &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        KeyValue nmlPayload;

        /** set payload */
        void payload(const KeyValue &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<KeyValue> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        KeyValue * payload() { return & nmlPayload; };

    //     std::shared_ptr<KeyValue> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for AddDiagnosticsRequest 

    */
    class AddDiagnosticsRequestNML : public NMLmsg
    {
    public:

        /** constructor */
        AddDiagnosticsRequestNML();
        /** destructor */
        ~AddDiagnosticsRequestNML();

        /** assignment operator */
        AddDiagnosticsRequestNML & operator=(const AddDiagnosticsRequestNML &inobj);
        /** assignment operator for base data class */
        AddDiagnosticsRequestNML & operator=(const AddDiagnosticsRequest &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        AddDiagnosticsRequest nmlPayload;

        /** set payload */
        void payload(const AddDiagnosticsRequest &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<AddDiagnosticsRequest> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        AddDiagnosticsRequest * payload() { return & nmlPayload; };

    //     std::shared_ptr<AddDiagnosticsRequest> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for AddDiagnosticsResponse 

    */
    class AddDiagnosticsResponseNML : public NMLmsg
    {
    public:

        /** constructor */
        AddDiagnosticsResponseNML();
        /** destructor */
        ~AddDiagnosticsResponseNML();

        /** assignment operator */
        AddDiagnosticsResponseNML & operator=(const AddDiagnosticsResponseNML &inobj);
        /** assignment operator for base data class */
        AddDiagnosticsResponseNML & operator=(const AddDiagnosticsResponse &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        AddDiagnosticsResponse nmlPayload;

        /** set payload */
        void payload(const AddDiagnosticsResponse &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<AddDiagnosticsResponse> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        AddDiagnosticsResponse * payload() { return & nmlPayload; };

    //     std::shared_ptr<AddDiagnosticsResponse> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for AddDiagnostics 

    */
    class AddDiagnosticsNML : public NMLmsg
    {
    public:

        /** constructor */
        AddDiagnosticsNML();
        /** destructor */
        ~AddDiagnosticsNML();

        /** assignment operator */
        AddDiagnosticsNML & operator=(const AddDiagnosticsNML &inobj);
        /** assignment operator for base data class */
        AddDiagnosticsNML & operator=(const AddDiagnostics &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        AddDiagnostics nmlPayload;

        /** set payload */
        void payload(const AddDiagnostics &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<AddDiagnostics> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        AddDiagnostics * payload() { return & nmlPayload; };

    //     std::shared_ptr<AddDiagnostics> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for SelfTestRequest 

    */
    class SelfTestRequestNML : public NMLmsg
    {
    public:

        /** constructor */
        SelfTestRequestNML();
        /** destructor */
        ~SelfTestRequestNML();

        /** assignment operator */
        SelfTestRequestNML & operator=(const SelfTestRequestNML &inobj);
        /** assignment operator for base data class */
        SelfTestRequestNML & operator=(const SelfTestRequest &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        SelfTestRequest nmlPayload;

        /** set payload */
        void payload(const SelfTestRequest &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<SelfTestRequest> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        SelfTestRequest * payload() { return & nmlPayload; };

    //     std::shared_ptr<SelfTestRequest> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for SelfTestResponse 

    */
    class SelfTestResponseNML : public NMLmsg
    {
    public:

        /** constructor */
        SelfTestResponseNML();
        /** destructor */
        ~SelfTestResponseNML();

        /** assignment operator */
        SelfTestResponseNML & operator=(const SelfTestResponseNML &inobj);
        /** assignment operator for base data class */
        SelfTestResponseNML & operator=(const SelfTestResponse &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        SelfTestResponse nmlPayload;

        /** set payload */
        void payload(const SelfTestResponse &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<SelfTestResponse> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        SelfTestResponse * payload() { return & nmlPayload; };

    //     std::shared_ptr<SelfTestResponse> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for SelfTest 

    */
    class SelfTestNML : public NMLmsg
    {
    public:

        /** constructor */
        SelfTestNML();
        /** destructor */
        ~SelfTestNML();

        /** assignment operator */
        SelfTestNML & operator=(const SelfTestNML &inobj);
        /** assignment operator for base data class */
        SelfTestNML & operator=(const SelfTest &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        SelfTest nmlPayload;

        /** set payload */
        void payload(const SelfTest &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<SelfTest> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        SelfTest * payload() { return & nmlPayload; };

    //     std::shared_ptr<SelfTest> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    // predeclaration of nmlupdate functions (necessary due to namespaces )

    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, diagnostic_msgs::DiagnosticArray & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, diagnostic_msgs::DiagnosticStatus & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, diagnostic_msgs::KeyValue & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, diagnostic_msgs::AddDiagnosticsRequest & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, diagnostic_msgs::AddDiagnosticsResponse & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, diagnostic_msgs::AddDiagnostics & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, diagnostic_msgs::SelfTestRequest & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, diagnostic_msgs::SelfTestResponse & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, diagnostic_msgs::SelfTest & inobj);

    /** nml format function */
    int diagnostic_msgs_gencpp_format(NMLTYPE type, void * buffer, CMS* cms);

}; // end namespace diagnostic_msgs

#endif // _DIAGNOSTIC_MSGS_GENCPPNML_H_
