// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         alex on alex-laptop, 2016-11-23 12:44:47 -0500
//  From Template/Script: data_templates/DataClassTemplate.cpp
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#include <math.h> // to allow use of common constants...
#include <common/Error.h>
#include "control_msgs/QueryTrajectoryStateResponse.h"

#include <common/JsonIo.h> // include here so container types are included by the class header 

using namespace std;

const unsigned int control_msgs::QueryTrajectoryStateResponse::ID;
const unsigned int control_msgs::QueryTrajectoryStateResponse::DATA_VERSION;
const std::string control_msgs::QueryTrajectoryStateResponse::DATA_TYPE_NAME = "QueryTrajectoryStateResponse";
const std::string control_msgs::QueryTrajectoryStateResponse::DATA_TYPE_FULL_NAME = "control_msgs::QueryTrajectoryStateResponse";
const std::string control_msgs::QueryTrajectoryStateResponse::VERSION_MD5 = "b5ae07cd758fd4cc6e576db494d8471b";
/* xml source for this class */
const std::string control_msgs::QueryTrajectoryStateResponse::XML_SOURCE = "<class name=\"QueryTrajectoryStateResponse\" publicDataMembers=\"true\" sourceIDL=\"ROS\" ros=\"true\" comment=\"\" id=\"1246705472\">\
  <field name=\"name\" type=\"vector(string)\" sourceType=\"string\" comment=\"\"/>\
  <field name=\"position\" type=\"vector(double)\" sourceType=\"float64\" comment=\"\"/>\
  <field name=\"velocity\" type=\"vector(double)\" sourceType=\"float64\" comment=\"\"/>\
  <field name=\"acceleration\" type=\"vector(double)\" sourceType=\"float64\" comment=\"\"/>\
</class>";

control_msgs::QueryTrajectoryStateResponse::QueryTrajectoryStateResponse() : name(), position(), velocity(), acceleration()
{  
};

control_msgs::QueryTrajectoryStateResponse::QueryTrajectoryStateResponse(const control_msgs::QueryTrajectoryStateResponse & inobj) : name(inobj.name), position(inobj.position), velocity(inobj.velocity), acceleration(inobj.acceleration)
{
};

control_msgs::QueryTrajectoryStateResponse::~QueryTrajectoryStateResponse()
{
};

control_msgs::QueryTrajectoryStateResponse * control_msgs::QueryTrajectoryStateResponse::New()
{ 
    return new QueryTrajectoryStateResponse();
};

bool control_msgs::QueryTrajectoryStateResponse::dataTypeFlat() 
{   
    return false; 
};

control_msgs::QueryTrajectoryStateResponse & control_msgs::QueryTrajectoryStateResponse::operator=(const control_msgs::QueryTrajectoryStateResponse & inobj)
{
    if (this != &inobj)
    {

        // if datatype is flat, optimize with memcpy (complier level optimiation)
              /*  if (dataTypeFlat() == true)
                  REMOVED invalid when assigning from a parent type as this is the pointer to the derived type, thus control_msgs::QueryTrajectoryStateResponse is copied to the wrong location

        {
            memcpy(this,&inobj,sizeof(control_msgs::QueryTrajectoryStateResponse));
        }
        else */ 
        {

            name = inobj.name;
            position = inobj.position;
            velocity = inobj.velocity;
            acceleration = inobj.acceleration;
        }
    }

    return *this;
}

bool control_msgs::QueryTrajectoryStateResponse::operator==(const control_msgs::QueryTrajectoryStateResponse & inobj) const
{
    bool ret = true;

    if (this != &inobj)
    { 
        ret = ret 
             && (name == inobj.name)
             && (position == inobj.position)
             && (velocity == inobj.velocity)
             && (acceleration == inobj.acceleration);
    }

    return ret;
}

bool control_msgs::QueryTrajectoryStateResponse::operator!=(const control_msgs::QueryTrajectoryStateResponse & inobj) const
{
    bool ret = false;

    if (this != &inobj)
    {
        ret = !operator==(inobj);
    }

    return ret;
}

bool control_msgs::QueryTrajectoryStateResponse::operator<(const control_msgs::QueryTrajectoryStateResponse & inobj) const
{
    bool ret = false;

    if (this == &inobj)
    {
        ret = false;
    }
    else
    {  
        // comparison algorithm based on std::lexicographical_compare algorithm

        if (name < inobj.name) return true;
        if (inobj.name <  name) return false; 
        if (position < inobj.position) return true;
        if (inobj.position <  position) return false; 
        if (velocity < inobj.velocity) return true;
        if (inobj.velocity <  velocity) return false; 
        if (acceleration < inobj.acceleration) return true;
        if (inobj.acceleration <  acceleration) return false; ;
    }

    return ret;
}

bool control_msgs::QueryTrajectoryStateResponse::operator>(const control_msgs::QueryTrajectoryStateResponse & inobj) const
{
    bool ret = false;

    if (this == &inobj)
    {
        ret = false;
    }
    else
    {
        // comparison algorithm based on std::lexicographical_compare algorithm

        if (name > inobj.name) return true;
        if (inobj.name >  name) return false; 
        if (position > inobj.position) return true;
        if (inobj.position >  position) return false; 
        if (velocity > inobj.velocity) return true;
        if (inobj.velocity >  velocity) return false; 
        if (acceleration > inobj.acceleration) return true;
        if (inobj.acceleration >  acceleration) return false; ;
    }

    return ret;
}

template <> void rframe::json_io::encode(std::stringstream & sstr, const char * name, const control_msgs::QueryTrajectoryStateResponse & t, std::string & indent, bool lastItem)
{
    if ((name) && (*name != '\0')) rframe::json_io::encodeObjectStart(sstr,name,indent);

    rframe::json_io::encode(sstr,"name",t.name,indent,false);
    rframe::json_io::encode(sstr,"position",t.position,indent,false);
    rframe::json_io::encode(sstr,"velocity",t.velocity,indent,false);
    rframe::json_io::encode(sstr,"acceleration",t.acceleration,indent,true);

    if ((name) && (*name != '\0')) rframe::json_io::encodeObjectEnd(sstr,indent,lastItem);
}

std::string control_msgs::QueryTrajectoryStateResponse::toStr(bool pretty) const
{

    stringstream sstr;
    string indent = (pretty == true) ? "\n" : "";

    rframe::json_io::encodeObjectStartMain(sstr,indent);
    rframe::json_io::encode(sstr,"",*this,indent,true);
    rframe::json_io::encodeObjectEnd(sstr,indent,true); // false so as to  not add , at end of string

    return sstr.str();

};

std::basic_ostream<char>& operator<<(std::basic_ostream<char> &s, const control_msgs::QueryTrajectoryStateResponse & value)
{
    return s << value.toStr(false);
}

