// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         yongbo on yongbo-XPS-15-9550, 2017-01-26 15:57:04 -0500
//  From Template/Script: data_templates/DataClassTemplate.cpp
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#include <math.h> // to allow use of common constants...
#include <common/Error.h>
#include "control_msgs/QueryCalibrationStateResponse.h"

#include <common/JsonIo.h> // include here so container types are included by the class header 

using namespace std;

const unsigned int control_msgs::QueryCalibrationStateResponse::ID;
const unsigned int control_msgs::QueryCalibrationStateResponse::DATA_VERSION;
const std::string control_msgs::QueryCalibrationStateResponse::DATA_TYPE_NAME = "QueryCalibrationStateResponse";
const std::string control_msgs::QueryCalibrationStateResponse::DATA_TYPE_FULL_NAME = "control_msgs::QueryCalibrationStateResponse";
const std::string control_msgs::QueryCalibrationStateResponse::VERSION_MD5 = "0939fd4cc74f5a21127ca34c0bbb4864";
/* xml source for this class */
const std::string control_msgs::QueryCalibrationStateResponse::XML_SOURCE = "<class name=\"QueryCalibrationStateResponse\" publicDataMembers=\"true\" sourceIDL=\"ROS\" ros=\"true\" comment=\"\" id=\"1246705469\">\
  <field name=\"is_calibrated\" type=\"bool\" sourceType=\"bool\" comment=\"\"/>\
</class>";

control_msgs::QueryCalibrationStateResponse::QueryCalibrationStateResponse() : is_calibrated(0)
{  
};

control_msgs::QueryCalibrationStateResponse::QueryCalibrationStateResponse(const control_msgs::QueryCalibrationStateResponse & inobj) : is_calibrated(inobj.is_calibrated)
{
};

control_msgs::QueryCalibrationStateResponse::~QueryCalibrationStateResponse()
{
};

control_msgs::QueryCalibrationStateResponse * control_msgs::QueryCalibrationStateResponse::New()
{ 
    return new QueryCalibrationStateResponse();
};

bool control_msgs::QueryCalibrationStateResponse::dataTypeFlat() 
{  
    return true;
};

control_msgs::QueryCalibrationStateResponse & control_msgs::QueryCalibrationStateResponse::operator=(const control_msgs::QueryCalibrationStateResponse & inobj)
{
    if (this != &inobj)
    {

        // if datatype is flat, optimize with memcpy (complier level optimiation)
              /*  if (dataTypeFlat() == true)
                  REMOVED invalid when assigning from a parent type as this is the pointer to the derived type, thus control_msgs::QueryCalibrationStateResponse is copied to the wrong location

        {
            memcpy(this,&inobj,sizeof(control_msgs::QueryCalibrationStateResponse));
        }
        else */ 
        {

            is_calibrated = inobj.is_calibrated;
        }
    }

    return *this;
}

bool control_msgs::QueryCalibrationStateResponse::operator==(const control_msgs::QueryCalibrationStateResponse & inobj) const
{
    bool ret = true;

    if (this != &inobj)
    { 
        ret = ret 
             && (is_calibrated == inobj.is_calibrated);
    }

    return ret;
}

bool control_msgs::QueryCalibrationStateResponse::operator!=(const control_msgs::QueryCalibrationStateResponse & inobj) const
{
    bool ret = false;

    if (this != &inobj)
    {
        ret = !operator==(inobj);
    }

    return ret;
}

bool control_msgs::QueryCalibrationStateResponse::operator<(const control_msgs::QueryCalibrationStateResponse & inobj) const
{
    bool ret = false;

    if (this == &inobj)
    {
        ret = false;
    }
    else
    {  
        // comparison algorithm based on std::lexicographical_compare algorithm

        if (is_calibrated < inobj.is_calibrated) return true;
        if (inobj.is_calibrated <  is_calibrated) return false; ;
    }

    return ret;
}

bool control_msgs::QueryCalibrationStateResponse::operator>(const control_msgs::QueryCalibrationStateResponse & inobj) const
{
    bool ret = false;

    if (this == &inobj)
    {
        ret = false;
    }
    else
    {
        // comparison algorithm based on std::lexicographical_compare algorithm

        if (is_calibrated > inobj.is_calibrated) return true;
        if (inobj.is_calibrated >  is_calibrated) return false; ;
    }

    return ret;
}

template <> void rframe::json_io::encode(std::stringstream & sstr, const char * name, const control_msgs::QueryCalibrationStateResponse & t, std::string & indent, bool lastItem)
{
    if ((name) && (*name != '\0')) rframe::json_io::encodeObjectStart(sstr,name,indent);

    rframe::json_io::encode(sstr,"is_calibrated",t.is_calibrated,indent,true);

    if ((name) && (*name != '\0')) rframe::json_io::encodeObjectEnd(sstr,indent,lastItem);
}

std::string control_msgs::QueryCalibrationStateResponse::toStr(bool pretty) const
{

    stringstream sstr;
    string indent = (pretty == true) ? "\n" : "";

    rframe::json_io::encodeObjectStartMain(sstr,indent);
    rframe::json_io::encode(sstr,"",*this,indent,true);
    rframe::json_io::encodeObjectEnd(sstr,indent,true); // false so as to  not add , at end of string

    return sstr.str();

};

std::basic_ostream<char>& operator<<(std::basic_ostream<char> &s, const control_msgs::QueryCalibrationStateResponse & value)
{
    return s << value.toStr(false);
}

