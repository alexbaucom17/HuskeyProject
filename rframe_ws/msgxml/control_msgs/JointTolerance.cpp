// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         yongbo on yongbo-XPS-15-9550, 2017-01-26 15:57:04 -0500
//  From Template/Script: data_templates/DataClassTemplate.cpp
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#include <math.h> // to allow use of common constants...
#include <common/Error.h>
#include "control_msgs/JointTolerance.h"

#include <common/JsonIo.h> // include here so container types are included by the class header 

using namespace std;

const unsigned int control_msgs::JointTolerance::ID;
const unsigned int control_msgs::JointTolerance::DATA_VERSION;
const std::string control_msgs::JointTolerance::DATA_TYPE_NAME = "JointTolerance";
const std::string control_msgs::JointTolerance::DATA_TYPE_FULL_NAME = "control_msgs::JointTolerance";
const std::string control_msgs::JointTolerance::VERSION_MD5 = "077c537c6c1022f34be13cd0b5513697";
/* xml source for this class */
const std::string control_msgs::JointTolerance::XML_SOURCE = "<class name=\"JointTolerance\" publicDataMembers=\"true\" sourceIDL=\"ROS\" ros=\"true\" rosMD5=\"f544fe9c16cf04547e135dd6063ff5be\" rosDesc=\"# The tolerances specify the amount the position, velocity, and\\n# accelerations can vary from the setpoints.  For example, in the case\\n# of trajectory control, when the actual position varies beyond\\n# (desired position + position tolerance), the trajectory goal may\\n# abort.\\n# \\n# There are two special values for tolerances:\\n#  * 0 - The tolerance is unspecified and will remain at whatever the default is\\n#  * -1 - The tolerance is \\&quot;erased\\&quot;.  If there was a default, the joint will be\\n#         allowed to move without restriction.\\n\\nstring name\\nfloat64 position  # in radians or meters (for a revolute or prismatic joint, respectively)\\nfloat64 velocity  # in rad/sec or m/sec\\nfloat64 acceleration  # in rad/sec^2 or m/sec^2\\n\" comment=\"The tolerances specify the amount the position, velocity, and  accelerations can vary from the setpoints.  For example, in the case  of trajectory control, when the actual position varies beyond  (desired position + position tolerance), the trajectory goal may  abort.    There are two special values for tolerances:   * 0 - The tolerance is unspecified and will remain at whatever the default is   * -1 - The tolerance is &quot;erased&quot;.  If there was a default, the joint will be          allowed to move without restriction.\" id=\"1246705445\">\
  <field name=\"name\" type=\"string\" sourceType=\"string\" comment=\"\"/>\
  <field name=\"position\" type=\"double\" sourceType=\"float64\" comment=\"in radians or meters (for a revolute or prismatic joint, respectively)\"/>\
  <field name=\"velocity\" type=\"double\" sourceType=\"float64\" comment=\"in rad/sec or m/sec\"/>\
  <field name=\"acceleration\" type=\"double\" sourceType=\"float64\" comment=\"in rad/sec^2 or m/sec^2\"/>\
</class>";

control_msgs::JointTolerance::JointTolerance() : name(), position(0), velocity(0), acceleration(0)
{  
};

control_msgs::JointTolerance::JointTolerance(const control_msgs::JointTolerance & inobj) : name(inobj.name), position(inobj.position), velocity(inobj.velocity), acceleration(inobj.acceleration)
{
};

control_msgs::JointTolerance::~JointTolerance()
{
};

control_msgs::JointTolerance * control_msgs::JointTolerance::New()
{ 
    return new JointTolerance();
};

bool control_msgs::JointTolerance::dataTypeFlat() 
{  
    return rframe::CodeGenTraits<std::string>::flat();

};

control_msgs::JointTolerance & control_msgs::JointTolerance::operator=(const control_msgs::JointTolerance & inobj)
{
    if (this != &inobj)
    {

        // if datatype is flat, optimize with memcpy (complier level optimiation)
              /*  if (dataTypeFlat() == true)
                  REMOVED invalid when assigning from a parent type as this is the pointer to the derived type, thus control_msgs::JointTolerance is copied to the wrong location

        {
            memcpy(this,&inobj,sizeof(control_msgs::JointTolerance));
        }
        else */ 
        {

            name = inobj.name;
            position = inobj.position;
            velocity = inobj.velocity;
            acceleration = inobj.acceleration;
        }
    }

    return *this;
}

bool control_msgs::JointTolerance::operator==(const control_msgs::JointTolerance & inobj) const
{
    bool ret = true;

    if (this != &inobj)
    { 
        ret = ret 
             && (name == inobj.name)
             && (position == inobj.position)
             && (velocity == inobj.velocity)
             && (acceleration == inobj.acceleration);
    }

    return ret;
}

bool control_msgs::JointTolerance::operator!=(const control_msgs::JointTolerance & inobj) const
{
    bool ret = false;

    if (this != &inobj)
    {
        ret = !operator==(inobj);
    }

    return ret;
}

bool control_msgs::JointTolerance::operator<(const control_msgs::JointTolerance & inobj) const
{
    bool ret = false;

    if (this == &inobj)
    {
        ret = false;
    }
    else
    {  
        // comparison algorithm based on std::lexicographical_compare algorithm

        if (name < inobj.name) return true;
        if (inobj.name <  name) return false; 
        if (position < inobj.position) return true;
        if (inobj.position <  position) return false; 
        if (velocity < inobj.velocity) return true;
        if (inobj.velocity <  velocity) return false; 
        if (acceleration < inobj.acceleration) return true;
        if (inobj.acceleration <  acceleration) return false; ;
    }

    return ret;
}

bool control_msgs::JointTolerance::operator>(const control_msgs::JointTolerance & inobj) const
{
    bool ret = false;

    if (this == &inobj)
    {
        ret = false;
    }
    else
    {
        // comparison algorithm based on std::lexicographical_compare algorithm

        if (name > inobj.name) return true;
        if (inobj.name >  name) return false; 
        if (position > inobj.position) return true;
        if (inobj.position >  position) return false; 
        if (velocity > inobj.velocity) return true;
        if (inobj.velocity >  velocity) return false; 
        if (acceleration > inobj.acceleration) return true;
        if (inobj.acceleration >  acceleration) return false; ;
    }

    return ret;
}

template <> void rframe::json_io::encode(std::stringstream & sstr, const char * name, const control_msgs::JointTolerance & t, std::string & indent, bool lastItem)
{
    if ((name) && (*name != '\0')) rframe::json_io::encodeObjectStart(sstr,name,indent);

    rframe::json_io::encode(sstr,"name",t.name,indent,false);
    rframe::json_io::encode(sstr,"position",t.position,indent,false);
    rframe::json_io::encode(sstr,"velocity",t.velocity,indent,false);
    rframe::json_io::encode(sstr,"acceleration",t.acceleration,indent,true);

    if ((name) && (*name != '\0')) rframe::json_io::encodeObjectEnd(sstr,indent,lastItem);
}

std::string control_msgs::JointTolerance::toStr(bool pretty) const
{

    stringstream sstr;
    string indent = (pretty == true) ? "\n" : "";

    rframe::json_io::encodeObjectStartMain(sstr,indent);
    rframe::json_io::encode(sstr,"",*this,indent,true);
    rframe::json_io::encodeObjectEnd(sstr,indent,true); // false so as to  not add , at end of string

    return sstr.str();

};

std::basic_ostream<char>& operator<<(std::basic_ostream<char> &s, const control_msgs::JointTolerance & value)
{
    return s << value.toStr(false);
}

