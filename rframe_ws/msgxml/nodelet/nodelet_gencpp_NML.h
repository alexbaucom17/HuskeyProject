// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         yongbo on yongbo-XPS-15-9550, 2017-01-26 15:57:06 -0500
//  From Template/Script: data_templates/NMLHeaderTemplate.h
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#ifndef _CODEGEN_NODELET_GENCPPNML_H_
#define _CODEGEN_NODELET_GENCPPNML_H_

#include <messaging/nml/NMLMessageFactory.h>
//#include <DataTypeInfo.h>
#include <messaging/Message.h>

#include "nodelet_gencpp_NMLSupport.h"

namespace rframe { class DataTypeInfo; };

#include "NodeletListRequest.h"
#include "NodeletListResponse.h"
#include "NodeletList.h"
#include "NodeletLoadRequest.h"
#include "NodeletLoadResponse.h"
#include "NodeletLoad.h"
#include "NodeletUnloadRequest.h"
#include "NodeletUnloadResponse.h"
#include "NodeletUnload.h"

namespace nodelet {

    /** definition of message factory for nodelet_gencpp */
    class nodelet_gencppNMLMessageFactory : public NMLMessageFactory
    {
    public:
        /** constructor */
        nodelet_gencppNMLMessageFactory();
        /** destructor */
        virtual ~nodelet_gencppNMLMessageFactory();

        /** see  rframe::MessageFactoryInterface */
        virtual void dataTypes(std::vector<DataTypeInfo> & ids);
        /** see  rframe::MessageFactoryInterface */
        virtual int dataType(NMLmsg & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<NMLmsg> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateTransport(NMLmsg * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int allocate(rframe::ID_TYPE id, std::shared_ptr<MessageBase> & msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int deallocateMessage(MessageBase * msg);
        /** see  rframe::MessageFactoryInterface */
        virtual int toTransport( MessageBase & src, NMLmsg & dst);
        /** see  rframe::MessageFactoryInterface */
        virtual int fromTransport( NMLmsg & src, MessageBase & dst);

        /** \return poitner to nml format function */
        virtual NML_FORMAT_PTR FormatPointer();
        /** run constructor for class of type id in the provided buffer */
        virtual int constructBuffer(rframe::ID_TYPE id, void * buffer);
        /** run destructor for class of type id in the buffer */
        virtual int destructBuffer(rframe::ID_TYPE id, void * buffer);
    };

    /** factory allocation function for use when not dynamically loading the factory library  */
    NMLMessageFactory * allocate_nodelet_gencpp();

    /** NML interface for NodeletListRequest 

    */
    class NodeletListRequestNML : public NMLmsg
    {
    public:

        /** constructor */
        NodeletListRequestNML();
        /** destructor */
        ~NodeletListRequestNML();

        /** assignment operator */
        NodeletListRequestNML & operator=(const NodeletListRequestNML &inobj);
        /** assignment operator for base data class */
        NodeletListRequestNML & operator=(const NodeletListRequest &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        NodeletListRequest nmlPayload;

        /** set payload */
        void payload(const NodeletListRequest &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<NodeletListRequest> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        NodeletListRequest * payload() { return & nmlPayload; };

    //     std::shared_ptr<NodeletListRequest> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for NodeletListResponse 

    */
    class NodeletListResponseNML : public NMLmsg
    {
    public:

        /** constructor */
        NodeletListResponseNML();
        /** destructor */
        ~NodeletListResponseNML();

        /** assignment operator */
        NodeletListResponseNML & operator=(const NodeletListResponseNML &inobj);
        /** assignment operator for base data class */
        NodeletListResponseNML & operator=(const NodeletListResponse &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        NodeletListResponse nmlPayload;

        /** set payload */
        void payload(const NodeletListResponse &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<NodeletListResponse> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        NodeletListResponse * payload() { return & nmlPayload; };

    //     std::shared_ptr<NodeletListResponse> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for NodeletList 

    */
    class NodeletListNML : public NMLmsg
    {
    public:

        /** constructor */
        NodeletListNML();
        /** destructor */
        ~NodeletListNML();

        /** assignment operator */
        NodeletListNML & operator=(const NodeletListNML &inobj);
        /** assignment operator for base data class */
        NodeletListNML & operator=(const NodeletList &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        NodeletList nmlPayload;

        /** set payload */
        void payload(const NodeletList &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<NodeletList> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        NodeletList * payload() { return & nmlPayload; };

    //     std::shared_ptr<NodeletList> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for NodeletLoadRequest 

    */
    class NodeletLoadRequestNML : public NMLmsg
    {
    public:

        /** constructor */
        NodeletLoadRequestNML();
        /** destructor */
        ~NodeletLoadRequestNML();

        /** assignment operator */
        NodeletLoadRequestNML & operator=(const NodeletLoadRequestNML &inobj);
        /** assignment operator for base data class */
        NodeletLoadRequestNML & operator=(const NodeletLoadRequest &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        NodeletLoadRequest nmlPayload;

        /** set payload */
        void payload(const NodeletLoadRequest &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<NodeletLoadRequest> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        NodeletLoadRequest * payload() { return & nmlPayload; };

    //     std::shared_ptr<NodeletLoadRequest> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for NodeletLoadResponse 

    */
    class NodeletLoadResponseNML : public NMLmsg
    {
    public:

        /** constructor */
        NodeletLoadResponseNML();
        /** destructor */
        ~NodeletLoadResponseNML();

        /** assignment operator */
        NodeletLoadResponseNML & operator=(const NodeletLoadResponseNML &inobj);
        /** assignment operator for base data class */
        NodeletLoadResponseNML & operator=(const NodeletLoadResponse &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        NodeletLoadResponse nmlPayload;

        /** set payload */
        void payload(const NodeletLoadResponse &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<NodeletLoadResponse> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        NodeletLoadResponse * payload() { return & nmlPayload; };

    //     std::shared_ptr<NodeletLoadResponse> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for NodeletLoad 

    */
    class NodeletLoadNML : public NMLmsg
    {
    public:

        /** constructor */
        NodeletLoadNML();
        /** destructor */
        ~NodeletLoadNML();

        /** assignment operator */
        NodeletLoadNML & operator=(const NodeletLoadNML &inobj);
        /** assignment operator for base data class */
        NodeletLoadNML & operator=(const NodeletLoad &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        NodeletLoad nmlPayload;

        /** set payload */
        void payload(const NodeletLoad &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<NodeletLoad> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        NodeletLoad * payload() { return & nmlPayload; };

    //     std::shared_ptr<NodeletLoad> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for NodeletUnloadRequest 

    */
    class NodeletUnloadRequestNML : public NMLmsg
    {
    public:

        /** constructor */
        NodeletUnloadRequestNML();
        /** destructor */
        ~NodeletUnloadRequestNML();

        /** assignment operator */
        NodeletUnloadRequestNML & operator=(const NodeletUnloadRequestNML &inobj);
        /** assignment operator for base data class */
        NodeletUnloadRequestNML & operator=(const NodeletUnloadRequest &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        NodeletUnloadRequest nmlPayload;

        /** set payload */
        void payload(const NodeletUnloadRequest &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<NodeletUnloadRequest> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        NodeletUnloadRequest * payload() { return & nmlPayload; };

    //     std::shared_ptr<NodeletUnloadRequest> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for NodeletUnloadResponse 

    */
    class NodeletUnloadResponseNML : public NMLmsg
    {
    public:

        /** constructor */
        NodeletUnloadResponseNML();
        /** destructor */
        ~NodeletUnloadResponseNML();

        /** assignment operator */
        NodeletUnloadResponseNML & operator=(const NodeletUnloadResponseNML &inobj);
        /** assignment operator for base data class */
        NodeletUnloadResponseNML & operator=(const NodeletUnloadResponse &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        NodeletUnloadResponse nmlPayload;

        /** set payload */
        void payload(const NodeletUnloadResponse &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<NodeletUnloadResponse> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        NodeletUnloadResponse * payload() { return & nmlPayload; };

    //     std::shared_ptr<NodeletUnloadResponse> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    /** NML interface for NodeletUnload 

    */
    class NodeletUnloadNML : public NMLmsg
    {
    public:

        /** constructor */
        NodeletUnloadNML();
        /** destructor */
        ~NodeletUnloadNML();

        /** assignment operator */
        NodeletUnloadNML & operator=(const NodeletUnloadNML &inobj);
        /** assignment operator for base data class */
        NodeletUnloadNML & operator=(const NodeletUnload &inobj);

        /** static flag, if true boost serialization should be used to serialize this message */
        static const bool useBoost = true;

        /** nml serialization access */
        void update( CMS*);

        /** message version, used to verify message versions match on read */
        unsigned int nmlVersion;
        /** timestamp of message creation */
        double nmlTimestamp;
        /** id of module which sent the message */
        rframe::ID_TYPE nmlModuleId;

        /** nml payload */
        NodeletUnload nmlPayload;

        /** set payload */
        void payload(const NodeletUnload &data) 
        {
            nmlPayload = data;
        };

        /** set payload from shared_ptr */
        void payload(std::shared_ptr<NodeletUnload> & data)
        {
            nmlPayload = *data.get();
        };

        /** \return pointer to nml payload */
        NodeletUnload * payload() { return & nmlPayload; };

    //     std::shared_ptr<NodeletUnload> nmlPayload; reminder that nmlPayload used to be a pointer
    // but the only way this could work well is if we knew to instantiate it due to dataTypeFlat()
    // which we cannot know until compile/run time...
    };

    // predeclaration of nmlupdate functions (necessary due to namespaces )

    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, nodelet::NodeletListRequest & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, nodelet::NodeletListResponse & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, nodelet::NodeletList & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, nodelet::NodeletLoadRequest & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, nodelet::NodeletLoadResponse & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, nodelet::NodeletLoad & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, nodelet::NodeletUnloadRequest & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, nodelet::NodeletUnloadResponse & inobj);
    /** serialize a message to/from a buffer 
     \param cms buffer data structure to be serailized to/from
     \param inobj object to be serialized*/
    void nmlupdate(CMS *cms, nodelet::NodeletUnload & inobj);

    /** nml format function */
    int nodelet_gencpp_format(NMLTYPE type, void * buffer, CMS* cms);

}; // end namespace nodelet

#endif // _NODELET_GENCPPNML_H_
