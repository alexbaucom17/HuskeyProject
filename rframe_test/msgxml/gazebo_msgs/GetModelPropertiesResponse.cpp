// 
//  AUTOGENERATED FILE:   do not edit
//
//  Generated by:         yongbo on yongbo-XPS-15-9550, 2016-11-23 17:33:56 -0500
//  From Template/Script: data_templates/DataClassTemplate.cpp
 
/*
  CTA RIGHTS

  This document contains information proprietary to General Dynamics
  Robotic Systems and is not to be reproduced, transmitted, transferred,
  or otherwise used without the express written consent of General
  Dynamics Robotic Systems except as described below.

  GOVERNMENT PURPOSE RIGHTS

  Contract No.:        W911NF-10-2-0016, Articles of Collaboration,
                       Article 5, Intellectual Property
  Contractor Name:     General Dynamics Robotic Systems, Inc. (GDRS)
  Contractor Address:  1231 Tech Court, Westminster, MD  21157

  Expiration Date - None

  Technical data and computer software first created by Member(s) in
  performance of the Agreement will be disclosed to the Government.
  General Dynamics Robotic Systems retains the entire right, title, and
  interest throughout the world to each subject invention subject to the
  provisions of this clause and 37 CFR Part 401, 32 CFR 32.36, and
  34 CFR 34.25. The Government shall have the right to obtain, reproduce,
  publish or otherwise use the work for Government purposes.

  NON-EXCLUSIVE LICENSE TO CONSORTIUM PARTICIPANTS
  Consortium Members and subawardees are defined as 'Participants'
  Under Article 5.3, Participants are granted a nonexclusive, royalty-
  free, non-sublicenseable, non-assignable, license to Consortium
  Intellectual Property for the limited purpose of performing tasks under
  The Cooperative Agreement, for Government purposes only, and to make and
  have the same made solely for such use.

*/

#include <math.h> // to allow use of common constants...
#include <common/Error.h>
#include "gazebo_msgs/GetModelPropertiesResponse.h"

#include <common/JsonIo.h> // include here so container types are included by the class header 

using namespace std;

const unsigned int gazebo_msgs::GetModelPropertiesResponse::ID;
const unsigned int gazebo_msgs::GetModelPropertiesResponse::DATA_VERSION;
const std::string gazebo_msgs::GetModelPropertiesResponse::DATA_TYPE_NAME = "GetModelPropertiesResponse";
const std::string gazebo_msgs::GetModelPropertiesResponse::DATA_TYPE_FULL_NAME = "gazebo_msgs::GetModelPropertiesResponse";
const std::string gazebo_msgs::GetModelPropertiesResponse::VERSION_MD5 = "41cd2fb57d3971b870baf68a01701756";
/* xml source for this class */
const std::string gazebo_msgs::GetModelPropertiesResponse::XML_SOURCE = "<class name=\"GetModelPropertiesResponse\" publicDataMembers=\"true\" sourceIDL=\"ROS\" ros=\"true\" comment=\"\" id=\"1781288144\">\
  <field name=\"parent_model_name\" type=\"string\" sourceType=\"string\" comment=\"parent model\"/>\
  <field name=\"canonical_body_name\" type=\"string\" sourceType=\"string\" comment=\"name of canonical body, body names are prefixed by model name, e.g. pr2::base_link\"/>\
  <field name=\"body_names\" type=\"vector(string)\" sourceType=\"string\" comment=\"list of bodies, body names are prefixed by model name, e.g. pr2::base_link\"/>\
  <field name=\"geom_names\" type=\"vector(string)\" sourceType=\"string\" comment=\"list of geoms\"/>\
  <field name=\"joint_names\" type=\"vector(string)\" sourceType=\"string\" comment=\"list of joints attached to the model\"/>\
  <field name=\"child_model_names\" type=\"vector(string)\" sourceType=\"string\" comment=\"list of child models\"/>\
  <field name=\"is_static\" type=\"bool\" sourceType=\"bool\" comment=\"returns true if model is static\"/>\
  <field name=\"success\" type=\"bool\" sourceType=\"bool\" comment=\"return true if get successful\"/>\
  <field name=\"status_message\" type=\"string\" sourceType=\"string\" comment=\"comments if available\"/>\
</class>";

gazebo_msgs::GetModelPropertiesResponse::GetModelPropertiesResponse() : parent_model_name(), canonical_body_name(), body_names(), geom_names(), joint_names(), child_model_names(), is_static(0), success(0), status_message()
{  
};

gazebo_msgs::GetModelPropertiesResponse::GetModelPropertiesResponse(const gazebo_msgs::GetModelPropertiesResponse & inobj) : parent_model_name(inobj.parent_model_name), canonical_body_name(inobj.canonical_body_name), body_names(inobj.body_names), geom_names(inobj.geom_names), joint_names(inobj.joint_names), child_model_names(inobj.child_model_names), is_static(inobj.is_static), success(inobj.success), status_message(inobj.status_message)
{
};

gazebo_msgs::GetModelPropertiesResponse::~GetModelPropertiesResponse()
{
};

gazebo_msgs::GetModelPropertiesResponse * gazebo_msgs::GetModelPropertiesResponse::New()
{ 
    return new GetModelPropertiesResponse();
};

bool gazebo_msgs::GetModelPropertiesResponse::dataTypeFlat() 
{   
    return false; 
};

gazebo_msgs::GetModelPropertiesResponse & gazebo_msgs::GetModelPropertiesResponse::operator=(const gazebo_msgs::GetModelPropertiesResponse & inobj)
{
    if (this != &inobj)
    {

        // if datatype is flat, optimize with memcpy (complier level optimiation)
              /*  if (dataTypeFlat() == true)
                  REMOVED invalid when assigning from a parent type as this is the pointer to the derived type, thus gazebo_msgs::GetModelPropertiesResponse is copied to the wrong location

        {
            memcpy(this,&inobj,sizeof(gazebo_msgs::GetModelPropertiesResponse));
        }
        else */ 
        {

            parent_model_name = inobj.parent_model_name;
            canonical_body_name = inobj.canonical_body_name;
            body_names = inobj.body_names;
            geom_names = inobj.geom_names;
            joint_names = inobj.joint_names;
            child_model_names = inobj.child_model_names;
            is_static = inobj.is_static;
            success = inobj.success;
            status_message = inobj.status_message;
        }
    }

    return *this;
}

bool gazebo_msgs::GetModelPropertiesResponse::operator==(const gazebo_msgs::GetModelPropertiesResponse & inobj) const
{
    bool ret = true;

    if (this != &inobj)
    { 
        ret = ret 
             && (parent_model_name == inobj.parent_model_name)
             && (canonical_body_name == inobj.canonical_body_name)
             && (body_names == inobj.body_names)
             && (geom_names == inobj.geom_names)
             && (joint_names == inobj.joint_names)
             && (child_model_names == inobj.child_model_names)
             && (is_static == inobj.is_static)
             && (success == inobj.success)
             && (status_message == inobj.status_message);
    }

    return ret;
}

bool gazebo_msgs::GetModelPropertiesResponse::operator!=(const gazebo_msgs::GetModelPropertiesResponse & inobj) const
{
    bool ret = false;

    if (this != &inobj)
    {
        ret = !operator==(inobj);
    }

    return ret;
}

bool gazebo_msgs::GetModelPropertiesResponse::operator<(const gazebo_msgs::GetModelPropertiesResponse & inobj) const
{
    bool ret = false;

    if (this == &inobj)
    {
        ret = false;
    }
    else
    {  
        // comparison algorithm based on std::lexicographical_compare algorithm

        if (parent_model_name < inobj.parent_model_name) return true;
        if (inobj.parent_model_name <  parent_model_name) return false; 
        if (canonical_body_name < inobj.canonical_body_name) return true;
        if (inobj.canonical_body_name <  canonical_body_name) return false; 
        if (body_names < inobj.body_names) return true;
        if (inobj.body_names <  body_names) return false; 
        if (geom_names < inobj.geom_names) return true;
        if (inobj.geom_names <  geom_names) return false; 
        if (joint_names < inobj.joint_names) return true;
        if (inobj.joint_names <  joint_names) return false; 
        if (child_model_names < inobj.child_model_names) return true;
        if (inobj.child_model_names <  child_model_names) return false; 
        if (is_static < inobj.is_static) return true;
        if (inobj.is_static <  is_static) return false; 
        if (success < inobj.success) return true;
        if (inobj.success <  success) return false; 
        if (status_message < inobj.status_message) return true;
        if (inobj.status_message <  status_message) return false; ;
    }

    return ret;
}

bool gazebo_msgs::GetModelPropertiesResponse::operator>(const gazebo_msgs::GetModelPropertiesResponse & inobj) const
{
    bool ret = false;

    if (this == &inobj)
    {
        ret = false;
    }
    else
    {
        // comparison algorithm based on std::lexicographical_compare algorithm

        if (parent_model_name > inobj.parent_model_name) return true;
        if (inobj.parent_model_name >  parent_model_name) return false; 
        if (canonical_body_name > inobj.canonical_body_name) return true;
        if (inobj.canonical_body_name >  canonical_body_name) return false; 
        if (body_names > inobj.body_names) return true;
        if (inobj.body_names >  body_names) return false; 
        if (geom_names > inobj.geom_names) return true;
        if (inobj.geom_names >  geom_names) return false; 
        if (joint_names > inobj.joint_names) return true;
        if (inobj.joint_names >  joint_names) return false; 
        if (child_model_names > inobj.child_model_names) return true;
        if (inobj.child_model_names >  child_model_names) return false; 
        if (is_static > inobj.is_static) return true;
        if (inobj.is_static >  is_static) return false; 
        if (success > inobj.success) return true;
        if (inobj.success >  success) return false; 
        if (status_message > inobj.status_message) return true;
        if (inobj.status_message >  status_message) return false; ;
    }

    return ret;
}

template <> void rframe::json_io::encode(std::stringstream & sstr, const char * name, const gazebo_msgs::GetModelPropertiesResponse & t, std::string & indent, bool lastItem)
{
    if ((name) && (*name != '\0')) rframe::json_io::encodeObjectStart(sstr,name,indent);

    rframe::json_io::encode(sstr,"parent_model_name",t.parent_model_name,indent,false);
    rframe::json_io::encode(sstr,"canonical_body_name",t.canonical_body_name,indent,false);
    rframe::json_io::encode(sstr,"body_names",t.body_names,indent,false);
    rframe::json_io::encode(sstr,"geom_names",t.geom_names,indent,false);
    rframe::json_io::encode(sstr,"joint_names",t.joint_names,indent,false);
    rframe::json_io::encode(sstr,"child_model_names",t.child_model_names,indent,false);
    rframe::json_io::encode(sstr,"is_static",t.is_static,indent,false);
    rframe::json_io::encode(sstr,"success",t.success,indent,false);
    rframe::json_io::encode(sstr,"status_message",t.status_message,indent,true);

    if ((name) && (*name != '\0')) rframe::json_io::encodeObjectEnd(sstr,indent,lastItem);
}

std::string gazebo_msgs::GetModelPropertiesResponse::toStr(bool pretty) const
{

    stringstream sstr;
    string indent = (pretty == true) ? "\n" : "";

    rframe::json_io::encodeObjectStartMain(sstr,indent);
    rframe::json_io::encode(sstr,"",*this,indent,true);
    rframe::json_io::encodeObjectEnd(sstr,indent,true); // false so as to  not add , at end of string

    return sstr.str();

};

std::basic_ostream<char>& operator<<(std::basic_ostream<char> &s, const gazebo_msgs::GetModelPropertiesResponse & value)
{
    return s << value.toStr(false);
}

